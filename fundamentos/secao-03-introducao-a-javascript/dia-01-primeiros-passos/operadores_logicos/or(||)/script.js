let weekDay = 'sabado';

if (weekDay === 'segunda-feira' || weekDay === 'terça-feira' || weekDay === 'quarta-feira' || weekDay === 'quinta-feira' || weekDay === 'sexta-feira') {
    console.log ('Hoje é dia de Trybe');
}

else {
    console.log ('Fim de semana, hora de descansar!');
}